import type { Metadata, Viewport } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"], display: "swap", variable: "--font-inter" });

export const metadata: Metadata = {
  title: "Hassam Saleem — Flutter & AI-enabled apps",
  description:
    "Portfolio of Hassam Saleem, Software Engineer (Flutter, MERN, AI-enabled features). Ask the assistant anything about his work — answers come from his documents, and it says so when it doesn't know.",
};

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  colorScheme: "light",
  themeColor: "#ffffff",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className={inter.variable}>
      <body>{children}</body>
    </html>
  );
}
